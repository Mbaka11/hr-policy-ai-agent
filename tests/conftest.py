"""Shared fixtures for HR Policy AI Agent tests."""

import sys
from pathlib import Path

# Ensure the project root is on sys.path so `src` imports work
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
